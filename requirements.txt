# Core dependencies
opencv-python>=4.8.0
numpy>=1.24.0
Pillow>=10.0.0

# Notebook UI
ipywidgets>=8.0.0
matplotlib>=3.7.0
jupyterlab>=4.0.0

# Vectorization engines — at least one is required:
#   Option A (recommended): install potrace externally
#     Windows : choco install potrace  OR  download from https://potrace.sourceforge.net
#     macOS   : brew install potrace
#     Linux   : sudo apt install potrace
#
#   Option B: Python-only fallback
# vtracer>=0.6.0

# Optional enhancements:
# opencv-contrib-python>=4.8.0   # fast skeletonization via ximgproc.thinning
# scikit-image>=0.21.0           # fallback skeletonization